{
  "$id": "https://raw.githubusercontent.com/phaiel/familiar-schema/main/docs/v3/schemas/types/config/CandidateKnowledgeBase.v1.json",
  "title": "Candidate Knowledge Base",
  "description": "The data source for generating candidate matches in a reconciliation task.",
  "type": "object",
  "properties": {
    "type": { "enum": ["file", "database_table", "api_endpoint"] },
    "path": { "type": "string", "description": "Path to the file, table name, or API URL." }
  },
  "required": ["type", "path"]
}